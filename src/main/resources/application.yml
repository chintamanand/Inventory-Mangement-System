spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ivms
    username: root
    password: admin
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080

app:
  jwtSecret: ezG1eTFsWBmpNKeU
  jwtExpirationMs: 86400000